# Excess Land Dashboard (Mock or FileMaker)

Modern HTML dashboard that renders FileMaker Requests via the Data API,
or via local mock data when you're offline.

## Quick start (mock data)

```bash
npm install
npm start
# open:
http://localhost:3000/?mock=1
```
